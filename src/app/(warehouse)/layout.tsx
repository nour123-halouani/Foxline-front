import SubAdminLayout from "@/app/components/layouts/WarehouseLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
    return <SubAdminLayout>{children}</SubAdminLayout>;
}
